# E2E test dependencies. Pin loosely — CI installs latest patch.
# Contract tests
pytest>=8.0
requests>=2.32
azure-identity>=1.17
openapi-spec-validator>=0.7
jsonschema>=4.22
PyYAML>=6.0

# Evaluations (Azure AI Evaluation SDK)
azure-ai-evaluation>=1.0
azure-ai-inference>=1.0

# Observability
azure-monitor-query>=1.3

# Policy-compliance harness uses `terraform` (external binary) + subprocess
# no extra Python deps
